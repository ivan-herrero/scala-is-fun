package com.intenthq.scalaisfun

object Calculator {

  // Implement the following functions, having in mind controlling possible errors.
  // If you need/want to change the return type of a function in order to control the errors feel free to do so.
  // The first `sum` has been already implemented as an example.

  def sum(x: Int, y: Int): Int = x + y

  def sum(xs: List[Int]): Int = ???

  def avg(xs: List[Int]): Int = ???

  def max(xs: List[Int]): Int = ???

  def min(xs: List[Int]): Int = ???
}
